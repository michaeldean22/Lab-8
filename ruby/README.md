#The program takes the current array and prints the elements listed.
#It also prints the current time.
#You must first sudo apt install Ruby to your console.
#The command to run the program is "File i/O.rb"
 
